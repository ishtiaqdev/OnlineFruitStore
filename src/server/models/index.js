const Payment       = require('./payment.model'),
Products            = require('./products.model'),
SelectedProducts    = require('./selectedproducts.model')
User                = require('./user.model');
Coupons             = require('./coupons.model');

module.exports = {
    Payment,
    Products,
    SelectedProducts,
    User,
    Coupons
}